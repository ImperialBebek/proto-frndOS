/** FRnD wordmark + AI dot — text-rendered to avoid short-lived Figma assets. */

export function FrndLogo() {
  return (
    <span className="relative inline-flex shrink-0 items-start gap-0.5 text-xl font-semibold leading-none tracking-[-0.8px] text-text-inverse">
      frndOS
      <span className="-mt-1 text-sm leading-none text-text-inverse">*</span>
    </span>
  );
}
